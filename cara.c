#include <stdio.h>
#include <string.h>
int main(void) {
    int i; int x; printf("Insira o tamanho da Array: "); scanf("%d", &x);
    int lista[x];
    for(i = 0; i < x; i++) {
        scanf("%d", &lista[x]);
    }
    printf("%d\n", lista[0, 1, 2, 3]);
    return 0;
}